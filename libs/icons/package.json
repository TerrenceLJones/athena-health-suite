{
  "name": "@athena/icons",
  "version": "0.0.1",
  "private": true,
  "description": "Framework-agnostic named icon registry (data only), generated from specs/designs/athena-icons.js. Rendered by @athena/ui's <Icon> on web.",
  "main": "src/index.ts",
  "scripts": {
    "generate": "node scripts/generate-icons.mjs",
    "lint": "eslint .",
    "type-check": "tsc --noEmit",
    "test": "vitest run",
    "build": "vite build"
  },
  "dependencies": {},
  "metadata": {
    "package_type": "shared",
    "regenerate": "Edit specs/designs/athena-icons.js, then run `pnpm --filter @athena/icons generate` to rebuild src/icon-registry.ts."
  }
}
